<%# Accepts: check (required) %>

<div id="header_stats" class="text-end">
  <div class="h4 mb-0"><%= check.currency == "USD" ? "$" : check.currency %><%= number_with_precision(check.grand_total, precision: 2) %></div>
  <div class="text-muted small"><%= check.participants.count %> people</div>
</div>
