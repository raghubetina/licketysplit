<%# Accepts: check (required) %>
<div id="after_discounts_total" class="card-footer py-3 px-3">
  <div class="d-flex justify-content-between">
    <strong>After discounts</strong>
    <strong><%= format_currency(check.subtotal - check.total_discounts, check.currency_symbol) %></strong>
  </div>
</div>
