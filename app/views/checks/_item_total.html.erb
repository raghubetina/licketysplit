<%# Accepts: check (required) %>
<div id="item_total" class="card-footer py-3 px-3">
  <div class="d-flex justify-content-between">
    <strong>Item total</strong>
    <strong><%= format_currency(check.subtotal, check.currency_symbol) %></strong>
  </div>
</div>
